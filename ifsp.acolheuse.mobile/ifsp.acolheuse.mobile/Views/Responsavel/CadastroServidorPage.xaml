<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="http://prismlibrary.com"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="ifsp.acolheuse.mobile.Views.Responsavel.CadastroServidorPage"
             xmlns:sfText="clr-namespace:Syncfusion.XForms.TextInputLayout;assembly=Syncfusion.Core.XForms"
             xmlns:maskededit="clr-namespace:Syncfusion.XForms.MaskedEdit;assembly=Syncfusion.SfMaskedEdit.XForms"
             Title="Dados do Servidor">
  <ContentPage.Resources>
    <ResourceDictionary>
      <ResourceDictionary.MergedDictionaries>
        
      </ResourceDictionary.MergedDictionaries>
    </ResourceDictionary>
  </ContentPage.Resources>
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Salvar"
                 Command="{Binding SalvarServidorCommand}" />
    </ContentPage.ToolbarItems>
    <ContentPage.Content>
        <AbsoluteLayout VerticalOptions="FillAndExpand"
                HorizontalOptions="FillAndExpand">
            <ScrollView AbsoluteLayout.LayoutBounds="0,0,1,1"
                AbsoluteLayout.LayoutFlags="All"
                VerticalOptions="FillAndExpand"
                HorizontalOptions="FillAndExpand">
                <StackLayout BackgroundColor="White"
                     Padding="10,10,10,10"
                     Spacing="5">
                    <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <Grid Grid.Row="0">
                                <sfText:SfTextInputLayout Grid.Column="0" ContainerType="Outlined" Hint="CPF" >
                                    <maskededit:SfMaskedEdit Value="{Binding Servidor.Cpf}" Keyboard="Numeric" MaskType="Text" Mask="000.000.000-00" Culture="{Binding MaskedEditCultureInfo}" />
                                </sfText:SfTextInputLayout>
                                <Label Text="É Orientador?" Grid.Column="1" HorizontalTextAlignment="End"/>
                                <Switch IsToggled="{Binding Servidor.IsProfessor}" Grid.Column="1" />
                            </Grid>


                            <sfText:SfTextInputLayout Grid.Row="1" ContainerType="Outlined" Hint="Nome" >
                                <Entry Text="{Binding Servidor.Nome}" />
                            </sfText:SfTextInputLayout>

                            <sfText:SfTextInputLayout Grid.Row="2" ContainerType="Outlined" Hint="Sobrenome" >
                                <Entry Text="{Binding Servidor.Sobrenome}" />
                            </sfText:SfTextInputLayout>

                            <Grid Grid.Row="3">
                                <sfText:SfTextInputLayout Grid.Column="0" ContainerType="Outlined" Hint="Telefone" HorizontalOptions="FillAndExpand">
                                    <maskededit:SfMaskedEdit Value="{Binding Servidor.Telefone}" Keyboard="Numeric" Mask="(00) 0000-0000" />
                                </sfText:SfTextInputLayout>
                                <sfText:SfTextInputLayout Grid.Column="1" ContainerType="Outlined" Hint="Celular" HorizontalOptions="FillAndExpand">
                                    <maskededit:SfMaskedEdit Value="{Binding Servidor.Celular}" Keyboard="Numeric" Mask="(00) 0 0000-0000" />
                                </sfText:SfTextInputLayout>
                            </Grid>

                            <sfText:SfTextInputLayout Grid.Row="4" ContainerType="Outlined" Hint="E-mail"  HorizontalOptions="FillAndExpand" ReserveSpaceForAssistiveLabels="True"  >
                                <Entry Text="{Binding Servidor.Email}" IsEnabled="{Binding IsAdmin}" />
                            </sfText:SfTextInputLayout>
                            <Grid Grid.Row="5">

                                <sfText:SfTextInputLayout Grid.Column="0" ContainerType="Outlined" Hint="Senha" HorizontalOptions="FillAndExpand" IsVisible="{Binding IsAdmin}">
                                    <Entry Text="{Binding Servidor.Senha}" IsPassword="True"/>
                                </sfText:SfTextInputLayout>
                            <sfText:SfTextInputLayout Grid.Column="1" ContainerType="Outlined" Hint="Confirmar senha"  HorizontalOptions="FillAndExpand" IsVisible="{Binding IsAdmin}">
                                    <Entry Text="{Binding Servidor.ConfirmarSenha}" IsPassword="True"/>
                                </sfText:SfTextInputLayout>
                            </Grid>
                        </Grid>
                </StackLayout>
            </ScrollView>
            <AbsoluteLayout BackgroundColor="#22000000"
                    AbsoluteLayout.LayoutBounds="0.5,0.5,1,1"
                    AbsoluteLayout.LayoutFlags="All"
                    IsVisible="{Binding IsBusy}">
                <ActivityIndicator Color="Red"
                           AbsoluteLayout.LayoutBounds="0.5,0.5,0.1,0.1"
                           AbsoluteLayout.LayoutFlags="All"
                           IsVisible="True"
                           IsRunning="True"/>
            </AbsoluteLayout>
        </AbsoluteLayout>
        

    </ContentPage.Content>
</ContentPage>